<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous" />
    <link rel="stylesheet" href="css/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Exo+2:wght@100&family=Lora:ital@1&display=swap" rel="stylesheet" />
    <title>HERO LIST</title>
  </head>

  <body>
    <h1><%= heroes.title %></h1>
    <table class="table table-striped">
      <tr>
        <br />
        <th>Id</th>
        <th>Name</th>
        <th>Role</th>
        <th>Action</th>
      </tr>
      <% for (let i = 0; i < heroes.data.length; i++) { %>
      <tr>
        <td><%= i + 1 %></td>
        <td><%= heroes.data[i].name %></td>
        <td><%= heroes.data[i].role %></td>
        <form onsubmit="alert('Are you sure?')" action="/hero/remove" method="post">
          <td>
            <a class="btn btn-primary" href="/hero/<%= heroes.data[i].id %>">Edit</a>
            <input type="text" hidden name="id" value="<%= heroes.data[i].id %>" />
            <button type="submit" class="btn btn-danger">Remove</button>
          </td>
        </form>
      </tr>
      <% } %>
    </table>
    <br />
    <br />
    <br />

    <h3>TAMBAHKAN HERO</h3>
    <form onsubmit="alert('Are you sure?')" action="/hero/add" method="post">
      <input type="text" name="name" placeholder="isi nama hero" />
      <input type="text" name="role" placeholder="isi role" />
      <br />
      <br />
      <input class="btn btn-info" type="submit" value="Tambah" />
    </form>
  </body>
</html>
